# Elite Terminal
Local Streamlit app that converts headlines, analyzes tickers, and writes AI audit logs.

## Run locally
1. python3 -m venv venv
2. source venv/bin/activate
3. pip install -r requirements.txt
4. streamlit run elite_terminal_adaptive.py
